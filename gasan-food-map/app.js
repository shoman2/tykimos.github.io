const colors={"한식":"#e74c3c","중식":"#f39c12","일식":"#3498db","양식":"#27ae60"};const map=L.map("map").setView([CENTER.lat,CENTER.lon],16);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19,attribution:"&copy; OpenStreetMap contributors"}).addTo(map);L.marker([CENTER.lat,CENTER.lon]).addTo(map).bindPopup(`<b>${CENTER.name}</b><br>${CENTER.address}`);L.circle([CENTER.lat,CENTER.lon],{radius:900,color:"#6f42c1",fillOpacity:.04,weight:2}).addTo(map);let layer=L.layerGroup().addTo(map);function icon(cat){return L.divIcon({className:"",html:`<div style="background:${colors[cat]};width:18px;height:18px;border-radius:50%;border:3px solid #fff;box-shadow:0 1px 6px #0006"></div>`,iconSize:[18,18],iconAnchor:[9,9]})}function render(cat="전체"){layer.clearLayers();const shown=PLACES.filter(p=>cat==="전체"||p.cat===cat);shown.forEach(p=>L.marker([p.lat,p.lon],{icon:icon(p.cat)}).addTo(layer).bindTooltip(`${p.cat} | ${p.name}`).bindPopup(`<b>${p.name}</b><br><b>${p.cat}</b> · ${p.score}<br>${p.menu}<br>${p.scene}<br><span style="color:#555">${p.review}</span><br><br>${p.address}<br><a target="_blank" href="${p.naver}">네이버지도</a> · <a target="_blank" href="${p.google}">구글지도</a> · <a target="_blank" href="${p.source_url}">근거</a>`));document.getElementById("list").innerHTML=`<table><thead><tr><th>분류</th><th>상호</th><th>메뉴</th><th>상황</th></tr></thead><tbody>${shown.map(p=>`<tr><td>${p.cat}</td><td>${p.name}</td><td>${p.menu}</td><td>${p.scene}</td></tr>`).join("")}</tbody></table>`}render();document.querySelectorAll("button[data-cat]").forEach(b=>b.onclick=()=>{document.querySelectorAll("button").forEach(x=>x.classList.remove("on"));b.classList.add("on");render(b.dataset.cat)});